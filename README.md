# sphinx-longmd docs project

This is a standalone Sphinx documentation project for `sphinx-longmd`.

## What is included

- a compact user-facing docs site
- pages for install, usage, output behavior, configuration, limitations, dialect, contributing, changelog, and license

## Build locally

```bash
pip install sphinx myst-parser
sphinx-build -b html source _build/html
```
